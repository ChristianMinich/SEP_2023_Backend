CREATE TABLE Seller(
    name VARCHAR(255),
    owner VARCHAR(255),
    street VARCHAR(255),
    house_number INT NOT NULL,
    zip_code INT NOT NULL,
    telephone_number INT NOT NULL,
    e_mail NVARCHAR(320)
);
